apiVersion: apps/v1
kind: Deployment
metadata:
  name: topic-service
  namespace: els-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: topic-service
  template:
    metadata:
      labels:
        app: topic-service
    spec:
      containers:
        - name: topic-service
          image: harishdell/els-ai-topic-service:sha-ad4a33a
          imagePullPolicy: Always
          ports:
            - containerPort: 4010
          env:
            - name: PORT
              value: "4010"
          envFrom:
            - configMapRef:
                name: els-ai-config
            - secretRef:
                name: els-ai-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: topic-service
  namespace: els-ai
spec:
  selector:
    app: topic-service
  ports:
    - name: http
      port: 4010
      targetPort: 4010
