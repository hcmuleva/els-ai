apiVersion: apps/v1
kind: Deployment
metadata:
  name: story-service
  namespace: els-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: story-service
  template:
    metadata:
      labels:
        app: story-service
    spec:
      containers:
        - name: story-service
          image: harishdell/els-ai-story-service:a9c3d2a28c8132873374c48d52417f1880637431
          imagePullPolicy: Always
          ports:
            - containerPort: 4014
          env:
            - name: PORT
              value: "4014"
          envFrom:
            - configMapRef:
                name: els-ai-config
            - secretRef:
                name: els-ai-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: story-service
  namespace: els-ai
spec:
  selector:
    app: story-service
  ports:
    - name: http
      port: 4014
      targetPort: 4014
