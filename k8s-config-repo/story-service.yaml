apiVersion: apps/v1
kind: Deployment
metadata:
  name: story-service
  namespace: els-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: story-service
  template:
    metadata:
      labels:
        app: story-service
    spec:
      containers:
        - name: story-service
          image: harishdell/els-ai-story-service:bce4262bef1c32124b42f7e3bc1c6fb9a2d3e5f6
          imagePullPolicy: Always
          ports:
            - containerPort: 4014
          env:
            - name: PORT
              value: "4014"
          envFrom:
            - configMapRef:
                name: els-ai-config
            - secretRef:
                name: els-ai-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: story-service
  namespace: els-ai
spec:
  selector:
    app: story-service
  ports:
    - name: http
      port: 4014
      targetPort: 4014
