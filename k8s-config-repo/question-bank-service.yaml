apiVersion: apps/v1
kind: Deployment
metadata:
  name: question-bank-service
  namespace: els-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: question-bank-service
  template:
    metadata:
      labels:
        app: question-bank-service
    spec:
      containers:
        - name: question-bank-service
          image: harishdell/els-ai-question-bank-service:7e67ff398705beeb5cbd1efed7760a02a7d8f47b
          imagePullPolicy: Always
          ports:
            - containerPort: 4008
          env:
            - name: PORT
              value: "4008"
          envFrom:
            - configMapRef:
                name: els-ai-config
            - secretRef:
                name: els-ai-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: question-bank-service
  namespace: els-ai
spec:
  selector:
    app: question-bank-service
  ports:
    - name: http
      port: 4008
      targetPort: 4008
