"""Generation helpers: difficulty rubric and prompt builders.

These are provider-agnostic. The Droid-authored provider uses the rubric text
for level metadata; the external LLM provider uses the full prompt builders.
"""
